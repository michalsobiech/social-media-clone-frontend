import { ReactElement } from "react";

/*
  If user entered the email address and then clicked 'Forgot ...'
  the email address should be passed to reset password form automatically
*/

export default function ForgotPassword(): ReactElement {
  return <div>ForgotPassword</div>;
}
